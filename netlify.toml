[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Ensures Prisma client is always regenerated (not served from cache)
  PRISMA_GENERATE_SKIP_AUTOINSTALL = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
